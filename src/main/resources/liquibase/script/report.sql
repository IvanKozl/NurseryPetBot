-- liquibase formatted sql

-- changeset Maria:5
CREATE TABLE nurserycatdogbot.report
(
id bigint GENERATED BY DEFAULT AS IDENTITY NOT NULL,
chat_id bigint,
food text,
feel text,
behavior text,
date_time DATE,
foto_check BOOLEAN,
report_check BOOLEAN,
user_number bigint REFERENCES nurserycatdogbot.users_contact_information (id),
PRIMARY KEY (id));


-- -- changeset Maria:51
-- Alter table nurserycatdogbot.report
-- add constraint report_pk primary key (id)
--;